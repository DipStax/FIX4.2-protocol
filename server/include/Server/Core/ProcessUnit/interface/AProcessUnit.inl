#include "Server/Core/ProcessUnit/interface/AProcessUnit.hpp"

namespace pu
{
    // template<class T>
    // AProcessUnit<T>::QueueInputType &AProcessUnit<T>::getInput()
    // {
    //     return m_input;
    // }

    // void AProcessUnit<T>::runtime(std::stop_token _st)
    // {
    //     while (_st.stop_requested())
    //         while (!m_input.empty())
    //             process(std::move(m_input.pop_front()));
    // }
}