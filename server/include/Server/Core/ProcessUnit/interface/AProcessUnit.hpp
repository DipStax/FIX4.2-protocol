#pragma once

#include "Server/Core/ProcessUnit/interface/IProcessUnit.hpp"

namespace pu
{
    // template<class T>
    // class AProcessUnit : public IProcessUnit<T>
    // {
    //     public:
    //         QueueInputType &getInput()

    //     protected:
    //         voir runtime(std::stop_token _st);

    //         virtual void process(T &&_data) = 0;

    //         QueueInputType m_input;
    // };
}